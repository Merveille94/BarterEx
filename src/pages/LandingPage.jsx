import Hero from "../component/Hero.jsx";

// Here is the landing page structure and content notice it only contains Hero that is because of the layout
const LandingPage = () => {
    return (
        <>
            <Hero/>
        </>
    )
}
export default LandingPage
